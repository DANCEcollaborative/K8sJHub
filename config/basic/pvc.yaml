apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jupyterhub-shared-pvc
  namespace: k8s-ns
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: azurefile
  resources:
    requests:
      storage: 10Gi
