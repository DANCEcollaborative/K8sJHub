version: '3.8'

services:
  jupyter-chat:
    build:
      context: .
      dockerfile: Dockerfile.chat
      args:
        CACHEBUST: ${CACHEBUST_ARG:-1}
    container_name: jupyter-chat
    ports:
      - "8888:8888"
    volumes:
      - ./shared:/home/jovyan/shared
    environment:
      - JUPYTER_ENABLE_LAB=yes
      - CHAT_WS_URL=http://chat-backend:3001
    user: "1000:100"
    command: start-notebook.py --collaborative --ServerApp.token='' --ServerApp.disable_check_xsrf=True --ServerApp.allow_origin='*'
    depends_on:
      - chat-backend

  chat-backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    container_name: chat-backend
    ports:
      - "3001:3001"
    # No bind mount to backend during container startup to avoid overwriting node_modules
    working_dir: /home/node/app
    command: node server.js
    environment:
      - NODE_ENV=development
